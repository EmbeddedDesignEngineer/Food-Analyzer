"""Thin shim so `python -m foodanalyzer` works, per TOPIC.md's CLI spec.
The real code lives in src/ (per TOPIC.md's recommended layout) — see
src/cli.py. This package exists only to give it that exact entry point.
"""
